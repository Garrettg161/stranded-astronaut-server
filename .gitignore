.env

# Maestro / Appium WebDriverAgent driver cache -- auto-downloaded and regenerated
# by maestro-runner; must never be committed (dropped here 2026-07-19 when a maestro
# run used this repo as its working directory). Root fix: run maestro with an explicit
# cwd=/tmp so it never litters a repo.
drivers/
.maestro-runner/

# Node dependencies (regenerated by npm install)
node_modules/
